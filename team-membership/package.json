{
  "name": "team-membership",
  "version": "1.0.0",
  "description": "GitHub Action to get a user team membership",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx ncc build src/index.js -m -o dist",
    "lint": "eslint .",
    "format": "prettier --write '**/*.{js,json,md,yml}'"
  },
  "keywords": [],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/stacks-network/actions"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^5.1.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1",
    "eslint": "^8.51.0",
    "prettier": "^3.0.3"
  }
}
